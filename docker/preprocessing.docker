FROM python:2.7

RUN apt-get update && apt-get install -y \
    awscli \
    nano \
    python-pip

RUN pip install \
    boto3 \
    enum34 \
    pandas \
    psycopg2 \
    scikit-learn

# Scipy is huge, install it separately
RUN pip install scipy

RUN mkdir /net /net/efs

WORKDIR /opt/biometrix/preprocessing

COPY ./app/src/Version_2.2 .

#ENTRYPOINT python
#CMD sessionProcess.py
